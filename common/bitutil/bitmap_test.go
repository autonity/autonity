package bitutil

import (
	"testing"
)

func TestBitmapItem(t *testing.T) {
	// TODO (tariq): test AddItem, HasItem, ItemCount, ItemList, Valid
}

func TestBitmapOps(t *testing.T) {
	// TODO (tariq): test Merge, Contains, CommonSet, UnionSet, ForEachItem
}
