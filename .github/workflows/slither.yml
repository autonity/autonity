name: Slither Analysis
on:
  workflow_dispatch:
  #pull_request:
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: crytic/slither-action@v0.4.1
        with:
          target: "autonity/solidity/contracts/"
          solc-version: "0.8.22"
