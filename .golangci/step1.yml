run:
  deadline: 10m

linters:
  enable:
    - goconst
    - gofmt
    - revive

  disable-all: true

linters-settings:
    goconst:
      min-len: 2
      min-occurrences: 2
    gofmt:
      auto-fix: false