run:
  deadline: 10m

linters:
  enable:
    - gosec
    - ineffassign
    - typecheck
  disable-all: true

issues:
  exclude:
    - 'G404'
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
