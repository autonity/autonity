ignore:
  - "**/*_mock.go"  # ignore mock files
coverage:
  status:
    project:
      default:
        target: 80%
        threshold: null
      tendermint:
        flags: tendermint
        target: 80%
flags:
  tendermint:
    paths:
      - consensus/tendermint
